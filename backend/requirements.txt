# Web layer
fastapi>=0.110
uvicorn[standard]>=0.27
python-multipart>=0.0.9

# Detection and tracking
ultralytics>=8.3.0
torch>=2.0
torchvision>=0.15
opencv-python>=4.8        # video decoding and the demo-clip generator
lap>=0.4                  # linear assignment, required by ByteTrack

# Imaging
pillow>=10.0
numpy>=1.24

# Geo Intelligence Assistant.
# Optional: without it (or without OPENAI_API_KEY) the assistant falls back to
# local lookups and says so. Everything else works unchanged.
openai>=1.40

# Tests only
httpx>=0.27               # required by fastapi.testclient
